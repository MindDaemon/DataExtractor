[project]
name = "network-security-icmp-lab"
version = "0.1.0"
description = "Educational ICMP sender/receiver scaffold for network security lab."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
